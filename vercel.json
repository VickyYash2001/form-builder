{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/angular",
      "config": {
        "outputDirectory": "dist/form-browser"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/form-browser/$1"
    }
  ]
}
